repositories {
    google()
    mavenCentral()
    maven {
	url 'https://artifactory-external.vkpartner.ru/artifactory/maven'
    }
}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    implementation 'ru.rustore.sdk:review:0.1.5'
    implementation 'ru.rustore.sdk:billingclient:1.0.0'
}
