repositories {
    maven {
        url = uri("https://artifactory-external.vkpartner.ru/artifactory/maven")
    }
}

dependencies {
    implementation("ru.rustore.sdk:review:0.1.4")
	implementation("ru.rustore.sdk:billingclient:0.1.7")
}